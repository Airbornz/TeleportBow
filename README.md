TeleportBow

This plugin allows players to teleport to where they shot an arrow, this should play nice with other plugins so that players don't end up where they shouldn't belong.

There is a config to allow you to set a permission (if wanted) for using this feature.

